import React from "react";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  gradient: {
    left: 0,
    right: 0,
    top: 0,
    height: 1000,
    position: "absolute"
  },
  icon: {
    top: 300,
    height: 30,
    backgroundColor: "red",
    textAlign: "center"
  }
});
